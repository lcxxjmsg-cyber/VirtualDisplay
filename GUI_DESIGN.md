# VirtualDisplay GUI 设计版图（v1 讨论稿）

> 目标：一个**美观、布局完善**的 Windows 原生 GUI，管理虚拟显示器的一切。
> 本文是设计沟通稿，未实现任何代码。请逐节审阅并反馈。

## ✅ 已确认决策（2026-08-06）

1. **技术路线**：Win32 原生 + GDI+ 自绘，**零第三方依赖**
2. **主色调**：蓝色 `#0E639C`
3. **HDR**：并入每个显示器卡片（行内开关），不设独立页
4. **托盘**：需要（最小化到托盘 + 托盘快速添加菜单）
5. **快速添加预设**：需要
6. **后续方向**：像 Windows 显示设置一样支持屏幕排版（位置/主屏），目标替代显示设置的主要功能

---

## 0. 技术路线（先决问题）

| 方案 | 美观度 | 工作量 | 依赖 |
| --- | --- | --- | --- |
| A. Win32 原生 + 自绘控件（GDI/Direct2D） | 可控，需大量自绘（圆角/阴影/动画） | 高 | 零依赖 |
| B. Win32 + 现代视觉（深色主题 + 自绘标题栏 + 卡片布局） | 高，接近 VS Code 观感 | 中高 | 零依赖 |
| C. 换技术栈（C# WPF/WinUI3 / Qt / WebView2） | 最高、最省力 | 低-中 | 引入运行时或 WebView2 |

> 你之前定了 C++ Win32/WTL。方案 B 是"保持 C++ 且能做得漂亮"的折中：
> 深色主题、自绘标题栏、卡片式布局、现代配色，全部用 GDI+/Direct2D 手绘，无第三方依赖。
> 若接受引入依赖，方案 C 的 WinUI3 或 WebView2 能把"漂亮"做到极致且快。
> **请先定技术路线**（默认推荐 B）。

---

## 1. 总体结构

- 窗口：1280×800，可缩放（最小 1040×680），深色主题（默认）+ 浅色主题（设置里切）
- 自绘标题栏（无系统边框）：应用名 + 驱动状态徽标 + 最小化/最大化/关闭
- 左侧导航栏（64px 图标栏 + 可展开文字）：
  **仪表盘 / 显示器 / 显卡 / 颜色·HDR / 自恢复 / 诊断 / 设置**
- 底部状态栏：`IddCx 1.10 rev 128 · 显示器 2/4 · 渲染: Intel UHD 630 · HDR 关`

```
┌──────────────────────────────────────────────────────────────────┐
│ ● VirtualDisplay                    [驱动已安装]      ─  □  ✕     │ ← 自绘标题栏
├──────────┬───────────────────────────────────────────────────────┤
│ ◧ 仪表盘  │                                                       │
│ ◨ 显示器  │            ┌─────────────────────────────┐            │
│ ◪ 显卡   │            │      (内容区，按导航切换)      │            │
│ ◫ 颜色HDR │            └─────────────────────────────┘            │
│ ◬ 自恢复  │                                                       │
│ ◭ 诊断   │                                                       │
│ ◮ 设置   │                                                       │
├──────────┴───────────────────────────────────────────────────────┤
│ IddCx 1.10 rev 128   ·   显示器 2/4   ·   渲染: Intel UHD 630   ·  HDR 关  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 2. 仪表盘（默认首页）

```
┌─────────────────────────────────────────────────────────────┐
│  欢迎回来，VirtualDisplay                [安装驱动] [卸载]     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │ 显示器 2/4│ │ 主显示器  │ │ 渲染 GPU  │ │ HDR      │        │
│  │ ● 活动   │ │ 2560×1440│ │ UHD 630  │ │ ● 关     │        │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │
│                                                              │
│  快速添加虚拟显示器                                             │
│  [1920×1080@60] [2560×1440@120] [3440×1440@100] [4K@60]      │
│  [+ 自定义…]                                                   │
│                                                              │
│  最近操作记录（只读滚动）                                       │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 08-06 10:23  Monitor 2 → 2560×1440@120  成功            │  │
│  │ 08-06 10:20  Monitor 1 added (1920×1080@60)            │  │
│  └────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

- 驱动未安装时：横幅提示 + 大按钮「安装驱动（需管理员）」，其余卡片置灰
- 统计卡片数据来自 `iddctrl caps / list / render-get / advancedcolor` 的 JSON

---

## 3. 显示器管理（核心页）

```
┌─────────────────────────────────────────────────────────────┐
│  虚拟显示器                          [+ 添加显示器] [全部移除]  │
│                                                              │
│  ┌─ 显示器 1 ──────────────────────────────┬─ 操作 ────────┐  │
│  │   ● 活动 · 主显示器                      │  [设为主屏]   │  │
│  │   分辨率  2560 × 1440   [▼]              │  [应用]      │  │
│  │   刷新率  120 Hz        [▼]              │  [移除]      │  │
│  │   HDR     [○ 开关]                       │              │  │
│  └──────────────────────────────────────────┴──────────────┘  │
│  ┌─ 显示器 2 ──────────────────────────────┬─ 操作 ────────┐  │
│  │   ○ 活动                               │  [设为主屏]   │  │
│  │   分辨率  1920 × 1080   [▼]             │  [应用]      │  │
│  │   刷新率  60 Hz         [▼]             │  [移除]      │  │
│  │   HDR     [○ 开关]                      │              │  │
│  └──────────────────────────────────────────┴──────────────┘  │
│                                                              │
│  迷你布局图（只读示意，按实际桌面比例）                          │
│   ┌──────────┐  ┌──────────┐                                  │
│   │  #1 2560  │  │  #2 1920  │                                 │
│   └──────────┘  └──────────┘                                  │
└─────────────────────────────────────────────────────────────┘
```

- 每张卡片 = 一个显示器（数据来自 `displayconfig` + `displays` 的 JSON）
- 分辨率/刷新率下拉 = `displays` 枚举出的模式表（12 模式），**自定义分辨率输入框**也有（驱动表外模式会提示"需加入模式表"）
- 「添加显示器」对话框：
```
┌─ 添加虚拟显示器 ───────────────────────┐
│  分辨率: [1920] × [1080]   [常用 ▼]   │
│  刷新率: [60] Hz            [常用 ▼]  │
│  渲染 GPU: [自动 ▼]                   │
│  ┌─────────────────────────────────┐  │
│  │  (实时预览: 1920×1080 @ 60Hz)   │  │
│  └─────────────────────────────────┘  │
│                [取消]  [添加]          │
└───────────────────────────────────────┘
```

---

## 4. 显卡（渲染 GPU）

```
┌─────────────────────────────────────────────────────────────┐
│  渲染适配器                                                    │
│                                                              │
│  ( ● ) 自动选择（高性能优先）                                   │
│  (   ) 手动指定：                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ ○ Intel(R) UHD Graphics 630   vendor=0x8086 dev=0x3E92 │  │
│  │ ○ NVIDIA GeForce RTX 4070     vendor=0x10DE dev=0x2786 │  │
│  │ ○ Microsoft Basic Render      vendor=0x1414 dev=0x008C │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  当前实际分配（swapchain）: Intel UHD 630 (LUID 0:22642)      │
│  [应用]  [刷新]                                               │
└─────────────────────────────────────────────────────────────┘
```

- 列表来自 `render-list --json`；选择后调 `render-set`；实际值来自 `render-get`
- 注：Windows 将选择视为偏好，实际分配可能不同——界面明确展示 requested vs actual

---

## 5. 颜色 · HDR

```
┌─────────────────────────────────────────────────────────────┐
│  颜色与 HDR                                                   │
│                                                              │
│  ┌─ 显示器 1 (2560×1440) ──────────────────────────────────┐  │
│  │   HDR10  ──────────────────────────────── [  ● 开  ]    │  │
│  │   SDR 内容亮度: [80] nits  (80–480)                     │  │
│  │   状态: 已支持 · 已激活 (10-bit WCG)                     │  │
│  └────────────────────────────────────────────────────────┘  │
│  说明: HDR 需要 IddCx 1.10 且显示器 EDID 携带 HDR 元数据；     │
│  若「已支持=否」可能是虚拟机显示栈限制。                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. 自恢复（持久化）

```
┌─────────────────────────────────────────────────────────────┐
│  配置持久化                                                    │
│                                                              │
│  [保存当前配置]   配置路径: C:\ProgramData\VirtualDisplay\     │
│                    monitors.conf (只读显示)                  │
│                                                              │
│  [ ] 登录后自动恢复显示器布局  （创建计划任务 VD-Restore）       │
│      ┌────────────────────────────────────────────────┐      │
│      │ 任务状态: ● 已注册 (SYSTEM, 登录触发)            │      │
│      │ 配置内容: 3 台显示器                            │      │
│      └────────────────────────────────────────────────┘      │
│  [立即恢复]  [立即清除全部]                                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 7. 诊断

```
┌─────────────────────────────────────────────────────────────┐
│  诊断                                                        │
│  [caps] [displayconfig] [displays] [paths] [dxgicap]         │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  (只读输出区，等宽字体，可复制)                          │  │
│  │  IddCx runtime: 1.10 rev 128                            │  │
│  │  hdr10: yes                                             │  │
│  └────────────────────────────────────────────────────────┘  │
│  [打开驱动日志]  [刷新]                                       │
└─────────────────────────────────────────────────────────────┘
```

---

## 8. 设置

```
┌─────────────────────────────────────────────────────────────┐
│  设置                                                        │
│  外观: [深色主题 ●]  [浅色主题 ○]                             │
│  语言: [简体中文] （占位，v1 仅中文）                          │
│  启动行为: [ ] 启动时最小化到托盘                                │
│  托盘图标: [ ] 关闭按钮最小化到托盘                              │
│  日志: C:\ProgramData\VirtualDisplay\VirtualDisplay.log  [打开] │
│  关于: VirtualDisplay v1.0.0 · IddCx 1.4–1.11 · MIT          │
└─────────────────────────────────────────────────────────────┘
```

---

## 9. 视觉规范（深色主题示例）

| 元素 | 值 |
| --- | --- |
| 背景 | `#1E1E1E`（VS Code 深色） |
| 卡片背景 | `#252526`，1px 边框 `#333333`，圆角 6px |
| 主色（按钮/开关/选中） | `#0E639C`（Win11 蓝）或 `#7C5CFF`（紫） |
| 成功/活动 | `#4EC9B0` / `#3FB950` |
| 警告/错误 | `#D29922` / `#F14C4C` |
| 正文/次要文字 | `#CCCCCC` / `#9D9D9D` |
| 字体 | 系统默认（Segoe UI Variable），等宽输出用 Consolas |

- 图标：导航用几何图标（自绘 16×16 路径），显示器状态用圆点 ●/○
- 动效：悬停高亮、开关过渡（200ms）、页面切换淡入（300ms）——全部 Direct2D 自绘

---

## 10. 交互细节

- 所有后台操作（add/update/install…）走 iddctrl 子进程 + `--json`，UI 解析 JSON 更新状态；操作期间按钮显示加载态并禁用重复点击
- install/uninstall 需要 UAC：GUI 用 `runas` 启动 iddctrl（与现在一致），完成后回读状态
- 若驱动未装：显示器页/显卡页/HDR 页显示"驱动未安装"空态插图 + 引导按钮
- 托盘：支持最小化到托盘，托盘菜单含「显示主界面 / 快速添加 1080p / 退出」
- 多实例保护：单实例运行（named mutex），二次启动聚焦已有窗口

---

## 待确认决策点

1. **技术路线**：A 纯 Win32 / **B 深色自绘（推荐）** / C 换栈（WinUI3 / WebView2 / Qt）
2. **主色调**：Win11 蓝 `#0E639C` / 紫 `#7C5CFF` / 其他
3. **页面取舍**：8 页是否过多？是否合并「颜色·HDR」进「显示器」卡片（每个显示器行内 HDR 开关）？
4. **托盘功能**：是否需要（最小化到托盘 + 快速添加）？
5. **分辨率预设**：快捷按钮放哪几个？（建议 1080p60 / 1440p120 / 3440x1440@100 / 4K60）
